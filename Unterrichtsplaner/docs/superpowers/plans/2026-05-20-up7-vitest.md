# UP-7 — vitest-Setup + Unit-Tests Implementation Plan

> **For agentic workers:** REQUIRED: Use superpowers:subagent-driven-development (if subagents available) or superpowers:executing-plans to implement this plan. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Ein vitest-Test-Fundament im Unterrichtsplaner aufbauen und die drei reinen Logik-Module (`generateColorVariants`, `gradeRequirements.ts`, `hkRotation.ts`) mit Unit-Tests absichern, inklusive eines Bugfixes und CI-Anbindung.

**Architecture:** vitest-Setup strukturell parallel zu ExamLab (jsdom + testing-library), colocated `*.test.ts`-Dateien neben den Quelldateien. Die Zielfunktionen sind reine Logik — Tests laufen ohne DOM. `npm test` wird CI-Gate in `deploy.yml` und im `pre-push`-Hook.

**Tech Stack:** vitest 4, jsdom, @testing-library/react + jest-dom, TypeScript, npm-Workspaces-Monorepo.

**Spec:** `Unterrichtsplaner/docs/superpowers/specs/2026-05-20-up7-vitest-design.md`

**Branch:** `feature/up7-vitest-tests` (bereits angelegt, Spec dort committet).

**Skills:** @superpowers:test-driven-development für Task 2 (echter Red→Green-Zyklus). Tasks 3–4 sind Charakterisierungstests für bereits korrekten Code — sie sollen beim ersten Lauf **grün** sein; ein roter Test bedeutet entweder einen Testfehler oder einen echten Defekt und muss untersucht werden.

---

## File Structure

**Neu:**
- `Unterrichtsplaner/vitest.config.ts` — vitest-Konfiguration.
- `Unterrichtsplaner/src/test-setup.ts` — Test-Setup (jest-dom-Matcher).
- `Unterrichtsplaner/src/data/categories.test.ts` — Tests für `generateColorVariants`.
- `Unterrichtsplaner/src/utils/gradeRequirements.test.ts` — Tests für die 5 Funktionen.
- `Unterrichtsplaner/src/utils/hkRotation.test.ts` — Tests für `getHKGroup`/`getHKSchedule`.

**Geändert:**
- `Unterrichtsplaner/package.json` — devDependencies + `test`-Scripts.
- `package-lock.json` (Repo-Root) — durch `npm install` aktualisiert.
- `Unterrichtsplaner/tsconfig.node.json` — `vitest.config.ts` in `include`.
- `Unterrichtsplaner/src/data/categories.ts` — Clamp-Fix in `generateColorVariants`.
- `.github/workflows/deploy.yml` — vitest-Step in Production + Staging.
- `.githooks/pre-push` — Planer-vitest-Schritt.
- `Unterrichtsplaner/HANDOFF.md` — UP-7 als erledigt markieren.

Alle Pfade relativ zum Repo-Root `GYM-WR-DUY/`. Befehle, die `npm test` / `tsc` / `build` aufrufen, laufen aus `Unterrichtsplaner/`, sofern nicht anders angegeben. `npm install` läuft aus dem **Repo-Root** (Workspaces).

---

## Task 1: vitest-Infrastruktur

**Files:**
- Modify: `Unterrichtsplaner/package.json`
- Create: `Unterrichtsplaner/vitest.config.ts`
- Create: `Unterrichtsplaner/src/test-setup.ts`
- Modify: `Unterrichtsplaner/tsconfig.node.json`

- [ ] **Step 1: devDependencies + Scripts in `Unterrichtsplaner/package.json` eintragen**

In `scripts` ergänzen (nach `"preview": "vite preview"`):
```json
    "test": "vitest run",
    "test:watch": "vitest"
```
In `devDependencies` ergänzen (alphabetisch einsortiert, Versionen identisch zu ExamLab):
```json
    "@testing-library/jest-dom": "^6.9.1",
    "@testing-library/react": "^16.3.2",
    "jsdom": "^29.0.1",
    "vitest": "^4.1.2"
```

- [ ] **Step 2: Dependencies installieren**

Run (aus dem **Repo-Root** `GYM-WR-DUY/`): `npm install`
Expected: Installation erfolgreich; `Unterrichtsplaner/package.json` und das Root-`package-lock.json` sind aktualisiert.

- [ ] **Step 3: `Unterrichtsplaner/vitest.config.ts` anlegen**

```ts
import { defineConfig } from 'vitest/config'
import react from '@vitejs/plugin-react'

export default defineConfig({
  plugins: [react()],
  resolve: {
    // npm-Workspaces hoisten alle Deps auf eine Root-node_modules.
    // dedupe sichert Single-Instance, sobald Komponenten-Tests dazukommen.
    dedupe: ['react', 'react-dom', 'zustand'],
  },
  test: {
    environment: 'jsdom',
    globals: true,
    setupFiles: ['./src/test-setup.ts'],
    include: ['src/**/*.test.{ts,tsx}'],
  },
})
```
Wichtig: Import aus `vitest/config`, **nicht** aus `vite` — nur ersterer liefert den getypten `test`-Schlüssel.

- [ ] **Step 4: `Unterrichtsplaner/src/test-setup.ts` anlegen**

```ts
import '@testing-library/jest-dom'
```

- [ ] **Step 5: `vitest.config.ts` in `tsconfig.node.json` aufnehmen**

In `Unterrichtsplaner/tsconfig.node.json` das `include`-Array ändern:
```json
  "include": ["vite.config.ts", "vitest.config.ts"]
```

- [ ] **Step 6: Infrastruktur verifizieren**

Run (aus `Unterrichtsplaner/`): `npx vitest run --passWithNoTests`
Expected: Exit 0 — vitest lädt die Config sauber, noch keine Testdateien.

Run (aus `Unterrichtsplaner/`): `npx tsc -b`
Expected: Exit 0, keine Fehler (test-setup.ts + vitest.config.ts typprüfen sauber).

- [ ] **Step 7: Commit**

```bash
git add Unterrichtsplaner/package.json package-lock.json Unterrichtsplaner/vitest.config.ts Unterrichtsplaner/src/test-setup.ts Unterrichtsplaner/tsconfig.node.json
git commit -m "test(planer): vitest-Infrastruktur (ExamLab-Paritaet)"
```

---

## Task 2: `generateColorVariants` — Test + Clamp-Fix (TDD)

Echter Red→Green-Zyklus: Der Test für satte Farben schlägt fehl, weil `bg` ohne Clamp einen ungültigen 9-stelligen Hex erzeugt. Danach wird der Fix angewendet.

**Files:**
- Create: `Unterrichtsplaner/src/data/categories.test.ts`
- Modify: `Unterrichtsplaner/src/data/categories.ts` (Funktion `generateColorVariants`, Zeilen 60–81)

- [ ] **Step 1: Testdatei `src/data/categories.test.ts` schreiben**

```ts
import { describe, it, expect } from 'vitest'
import { generateColorVariants } from './categories'

describe('generateColorVariants', () => {
  it('leitet bg/fg/border aus reinem Schwarz ab', () => {
    expect(generateColorVariants('#000000')).toEqual({
      bg: '#e6e6e6',
      fg: '#000000',
      border: '#4c4c4c',
    })
  })

  it('berechnet Varianten fuer eine mittlere Farbe (#3b82f6)', () => {
    expect(generateColorVariants('#3b82f6')).toEqual({
      bg: '#ecf3ff',
      fg: '#234e94',
      border: '#75a7f8',
    })
  })

  it('clampt bg-Kanaele bei einem 255-Kanal (#ff0000) auf gueltigen Hex', () => {
    const v = generateColorVariants('#ff0000')
    expect(v.bg).toBe('#ffe6e6')
    expect(v.bg).toMatch(/^#[0-9a-f]{6}$/)
  })

  it('erzeugt fuer reines Weiss gueltigen 7-Zeichen-Hex', () => {
    const v = generateColorVariants('#ffffff')
    expect(v.bg).toBe('#ffffff')
    expect(v.bg).toMatch(/^#[0-9a-f]{6}$/)
    expect(v.fg).toMatch(/^#[0-9a-f]{6}$/)
    expect(v.border).toMatch(/^#[0-9a-f]{6}$/)
  })
})
```

- [ ] **Step 2: Test laufen lassen — muss teilweise fehlschlagen**

Run (aus `Unterrichtsplaner/`): `npx vitest run src/data/categories.test.ts`
Expected: FAIL — die beiden letzten Tests (`#ff0000`, `#ffffff`) schlagen fehl, weil
`bg` ohne Clamp `#100e6e6` bzw. `#100100100` liefert. Die ersten beiden Tests
(`#000000`, `#3b82f6`) sind grün.

- [ ] **Step 3: Clamp-Fix in `generateColorVariants` anwenden**

In `src/data/categories.ts` die drei `bg`-Zeilen (aktuell Zeilen 65–67) ersetzen.

Vorher:
```ts
  // Light background: mix with white (90% white)
  const bgR = Math.round(r * 0.1 + 230);
  const bgG = Math.round(g * 0.1 + 230);
  const bgB = Math.round(b * 0.1 + 230);
```
Nachher:
```ts
  // Light background: mix with white (90% white), auf 255 geclampt
  // (analog zum border-Clamp unten — verhindert ungueltigen Hex bei 255-Kanaelen)
  const bgR = Math.min(255, Math.round(r * 0.1 + 230));
  const bgG = Math.min(255, Math.round(g * 0.1 + 230));
  const bgB = Math.min(255, Math.round(b * 0.1 + 230));
```

- [ ] **Step 4: Test laufen lassen — muss grün sein**

Run (aus `Unterrichtsplaner/`): `npx vitest run src/data/categories.test.ts`
Expected: PASS — alle 4 Tests grün.

- [ ] **Step 5: tsc verifizieren**

Run (aus `Unterrichtsplaner/`): `npx tsc -b`
Expected: Exit 0, keine Fehler.

- [ ] **Step 6: Commit**

```bash
git add Unterrichtsplaner/src/data/categories.ts Unterrichtsplaner/src/data/categories.test.ts
git commit -m "fix(planer): generateColorVariants bg-Kanaele auf 255 clampen + Tests"
```

---

## Task 3: `gradeRequirements.ts` — Unit-Tests

Charakterisierungstests für fünf bereits korrekte Funktionen. Erwartung: beim ersten Lauf grün.

**Files:**
- Create: `Unterrichtsplaner/src/utils/gradeRequirements.test.ts`

- [ ] **Step 1: Testdatei `src/utils/gradeRequirements.test.ts` schreiben**

```ts
import { describe, it, expect } from 'vitest'
import {
  getGymStufe,
  getGradeRequirements,
  getCourseGroups,
  countAssessments,
  checkGradeRequirements,
} from './gradeRequirements'
import type { Course, Week, LessonDetail } from '../types'

// Minimal-Fixture: deckt alle Pflichtfelder von Course ab.
function makeCourse(id: string, cls: string, typ: string, les: 1 | 2 | 3, col: number): Course {
  return { id, col, cls, typ, day: 'Mo', from: '08:00', to: '08:45', les, hk: false, semesters: [1, 2] }
}

describe('getGymStufe', () => {
  it('ordnet Klasse 29c bei SJ-Ende 2026 GYM1 zu', () => {
    expect(getGymStufe('29c')).toBe('GYM1')
  })
  it('nimmt die erste 2-stellige Zahl im Klassennamen', () => {
    expect(getGymStufe('28bc29fs')).toBe('GYM2')
    expect(getGymStufe('27a28f')).toBe('GYM3')
  })
  it('ordnet GYM4 und GYM5 (TaF) korrekt zu', () => {
    expect(getGymStufe('26d')).toBe('GYM4')
    expect(getGymStufe('25s')).toBe('GYM5')
  })
  it('liefert UNKNOWN bei fehlender Zahl oder zu grossem Abstand', () => {
    expect(getGymStufe('abc')).toBe('UNKNOWN')
    expect(getGymStufe('24x')).toBe('UNKNOWN')
  })
  it('respektiert den maturaYear-Override', () => {
    expect(getGymStufe('30a', 2027)).toBe('GYM1')
  })
})

describe('getGradeRequirements', () => {
  it('GYM1 liefert 3 Vorgaben in fester Reihenfolge', () => {
    const r = getGradeRequirements('GYM1', 3)
    expect(r.map(x => x.label)).toEqual([
      'Standortbestimmung (Nov)', 'Semesterzeugnis', 'Jahreszeugnis',
    ])
  })
  it('GYM1 mit >3 Wochenlektionen hebt das Jahreszeugnis-Minimum auf 4', () => {
    expect(getGradeRequirements('GYM1', 4).at(-1)!.minGrades).toBe(4)
    expect(getGradeRequirements('GYM1', 3).at(-1)!.minGrades).toBe(3)
  })
  it('GYM2-5 liefert 2 Vorgaben', () => {
    const r = getGradeRequirements('GYM3', 2)
    expect(r.map(x => x.label)).toEqual(['Zwischenbericht', 'Jahreszeugnis'])
  })
  it('GYM2 mit >3 Wochenlektionen hebt das Jahreszeugnis-Minimum auf 4', () => {
    expect(getGradeRequirements('GYM2', 5).at(-1)!.minGrades).toBe(4)
  })
})

describe('getCourseGroups', () => {
  it('gruppiert nach Klasse+Typ, summiert Wochenlektionen, dedupliziert kursIds', () => {
    const groups = getCourseGroups([
      makeCourse('a', '29c', 'SF', 2, 11),
      makeCourse('b', '29c', 'SF', 1, 13),
      makeCourse('c', '28c', 'IN', 1, 4),
    ])
    expect(groups).toHaveLength(2)
    const sf = groups.find(g => g.key === '29c-SF')!
    expect(sf.weeklyLessons).toBe(3)
    expect(sf.kursIds).toEqual(['a', 'b'])
    expect(sf.gymStufe).toBe('GYM1')
  })
})

describe('countAssessments', () => {
  const courses = [makeCourse('a', '29c', 'SF', 2, 11)]

  it('zaehlt Lektionen mit blockCategory ASSESSMENT', () => {
    const weeks: Week[] = [{ w: '33', lessons: { 11: { title: 'Pruefung', type: 1 } } }]
    const details: Record<string, LessonDetail> = { '33-11': { blockCategory: 'ASSESSMENT' } }
    expect(countAssessments(weeks, details, ['a'], courses, 'year', 1)).toBe(1)
  })
  it('zaehlt LessonEntry vom Typ 4 als Fallback', () => {
    const weeks: Week[] = [{ w: '33', lessons: { 11: { title: 'Pruefung', type: 4 } } }]
    expect(countAssessments(weeks, {}, ['a'], courses, 'year', 1)).toBe(1)
  })
  it('ignoriert Lektionen ohne Titel und fremde Spalten', () => {
    const weeks: Week[] = [
      { w: '33', lessons: { 11: { title: '', type: 4 }, 99: { title: 'X', type: 4 } } },
    ]
    expect(countAssessments(weeks, {}, ['a'], courses, 'year', 1)).toBe(0)
  })
  it('filtert nach Semester ueber s2StartIndex', () => {
    const weeks: Week[] = [
      { w: '33', lessons: { 11: { title: 'P1', type: 4 } } },
      { w: '34', lessons: { 11: { title: 'P2', type: 4 } } },
    ]
    expect(countAssessments(weeks, {}, ['a'], courses, 1, 1)).toBe(1)
    expect(countAssessments(weeks, {}, ['a'], courses, 2, 1)).toBe(1)
    expect(countAssessments(weeks, {}, ['a'], courses, 'year', 1)).toBe(2)
  })
})

describe('checkGradeRequirements', () => {
  it('berechnet Status und sortiert critical vor warning', () => {
    const courses = [makeCourse('a', '29c', 'SF', 3, 11)]
    const warnings = checkGradeRequirements([], {}, courses, 1)
    expect(warnings).toHaveLength(3)
    expect(warnings.map(w => w.status)).toEqual(['critical', 'critical', 'warning'])
  })
  it('ueberspringt Gruppen mit gymStufe UNKNOWN', () => {
    const courses = [makeCourse('x', 'abc', 'SF', 3, 11)]
    expect(checkGradeRequirements([], {}, courses, 1)).toEqual([])
  })
  it('meldet ok, wenn genug Beurteilungen geplant sind', () => {
    const courses = [makeCourse('a', '29c', 'SF', 3, 11)]
    const weeks: Week[] = [
      { w: '33', lessons: { 11: { title: 'P1', type: 4 } } },
      { w: '34', lessons: { 11: { title: 'P2', type: 4 } } },
      { w: '35', lessons: { 11: { title: 'P3', type: 4 } } },
    ]
    const warnings = checkGradeRequirements(weeks, {}, courses, 99)
    expect(warnings.every(w => w.status === 'ok')).toBe(true)
  })
})
```

- [ ] **Step 2: Tests laufen lassen**

Run (aus `Unterrichtsplaner/`): `npx vitest run src/utils/gradeRequirements.test.ts`
Expected: PASS — alle Tests grün. Falls ein Test rot ist: prüfen, ob die Fixture/Erwartung
fehlerhaft ist, oder ob ein echter Defekt vorliegt (dann melden, **nicht** den Test
an falsches Verhalten anpassen).

- [ ] **Step 3: tsc verifizieren**

Run (aus `Unterrichtsplaner/`): `npx tsc -b`
Expected: Exit 0 — die Fixtures sind typkorrekt.

- [ ] **Step 4: Commit**

```bash
git add Unterrichtsplaner/src/utils/gradeRequirements.test.ts
git commit -m "test(planer): Unit-Tests fuer gradeRequirements.ts"
```

---

## Task 4: `hkRotation.ts` — Unit-Tests

Charakterisierungstests. Die Alternations-Tests sind an `src/data/weeks.ts` gekoppelt
(Spalte 11 = 29c SF, Wochen w33–w35 mit je einer Unterrichtslektion type 1). Sollte
`weeks.ts` dort verändert werden, müssen die A/B-Erwartungen neu bestimmt werden.

**Files:**
- Create: `Unterrichtsplaner/src/utils/hkRotation.test.ts`

- [ ] **Step 1: Testdatei `src/utils/hkRotation.test.ts` schreiben**

```ts
import { describe, it, expect } from 'vitest'
import { getHKGroup, getHKSchedule } from './hkRotation'
import { WEEKS } from '../data/weeks'

describe('getHKGroup', () => {
  it('gibt den Override-Wert zurueck, wenn vorhanden', () => {
    expect(getHKGroup('33', 11, 'A', { '33-11': 'B' })).toBe('B')
  })
  it('liefert startGroup bei unbekannter Woche', () => {
    expect(getHKGroup('99', 11, 'A')).toBe('A')
    expect(getHKGroup('99', 11, 'B')).toBe('B')
  })
  it('alterniert A/B ueber aufeinanderfolgende Unterrichtswochen (Spalte 11)', () => {
    // gekoppelt an weeks.ts: Spalte 11 hat in w33-w35 je eine type-1-Lektion
    expect(getHKGroup('33', 11, 'A')).toBe('A')
    expect(getHKGroup('34', 11, 'A')).toBe('B')
    expect(getHKGroup('35', 11, 'A')).toBe('A')
  })
  it('kehrt das Muster mit startGroup B um', () => {
    expect(getHKGroup('33', 11, 'B')).toBe('B')
    expect(getHKGroup('34', 11, 'B')).toBe('A')
  })
})

describe('getHKSchedule', () => {
  it('liefert einen Eintrag pro Woche in WEEK_ORDER', () => {
    expect(getHKSchedule(11, 'A')).toHaveLength(WEEKS.length)
  })
  it('markiert Overrides mit isOverride und uebernimmt den Wert', () => {
    const schedule = getHKSchedule(11, 'A', { '33-11': 'B' })
    const w33 = schedule.find(e => e.week === '33')!
    expect(w33.isOverride).toBe(true)
    expect(w33.group).toBe('B')
    expect(schedule.find(e => e.week === '34')!.isOverride).toBe(false)
  })
})
```

- [ ] **Step 2: Tests laufen lassen**

Run (aus `Unterrichtsplaner/`): `npx vitest run src/utils/hkRotation.test.ts`
Expected: PASS. Falls die Alternations-Tests rot sind, zuerst gegen `weeks.ts`
gegenprüfen (Spalte 11, w33–w35) — siehe Kopfkommentar dieses Tasks.

- [ ] **Step 3: tsc + alle Tests verifizieren**

Run (aus `Unterrichtsplaner/`): `npx tsc -b && npm test`
Expected: tsc Exit 0; `npm test` grün — alle drei Testdateien zusammen.

- [ ] **Step 4: Commit**

```bash
git add Unterrichtsplaner/src/utils/hkRotation.test.ts
git commit -m "test(planer): Unit-Tests fuer hkRotation.ts"
```

---

## Task 5: CI-Anbindung — `deploy.yml` + `pre-push`

**Files:**
- Modify: `.github/workflows/deploy.yml`
- Modify: `.githooks/pre-push`

- [ ] **Step 1: vitest-Step im Production-Block von `deploy.yml`**

In `.github/workflows/deploy.yml`, direkt **nach** dem Step `Audit alert() Use (Planer)`
und **vor** `Audit \`any\` Use (Bundle L Gate)` einfügen:
```yaml
      - name: Run vitest (Planer)
        working-directory: Unterrichtsplaner
        run: npm test
```

- [ ] **Step 2: vitest-Step im Staging-Block von `deploy.yml`**

Direkt **nach** dem Step `Audit alert() Use (Planer, staging)` und **vor**
`Audit \`any\` Use (Bundle L Gate, staging)` einfügen:
```yaml
      - name: Run vitest (Planer, staging)
        if: steps.checkout-preview.outcome == 'success'
        working-directory: preview-src/Unterrichtsplaner
        run: npm test
```

- [ ] **Step 3: Planer-Tests in den `pre-push`-Hook aufnehmen**

In `.githooks/pre-push`, **nach** dem `packages/shared`-Lock-Sync-Block (endet mit
dessen `fi`) und **vor** dem Kommentar `# Step 2: Volle ci-check-Pipeline …`,
diesen Block einfügen:
```bash
# Step 2: Unterrichtsplaner vitest-Tests.
cd "$REPO_ROOT/Unterrichtsplaner"
echo "🔍 pre-push: Unterrichtsplaner vitest..."
if ! npm test; then
  echo "❌ Unterrichtsplaner: vitest-Tests fehlgeschlagen. Push blockiert."
  echo "   Override (mit Vorsicht): git push --no-verify"
  exit 1
fi
```
Anschliessend den bestehenden ExamLab-Kommentar von `# Step 2:` auf `# Step 3:`
umnummerieren (reine Kosmetik, Konsistenz).

- [ ] **Step 4: pre-push-Hook lokal verifizieren**

Run (aus dem Repo-Root): `bash .githooks/pre-push`
Expected: Der Hook durchläuft den shared-Lock-Check, dann die Planer-vitest-Tests
(grün), dann die ExamLab-ci-check. Abschluss-Meldung `✅`. (Der Lauf dauert einige
Minuten — die ExamLab-ci-check baut mit.)

- [ ] **Step 5: YAML-Syntax der Workflow-Datei prüfen**

Sicherstellen, dass die Einrückung der neuen Steps exakt zu den umgebenden Steps
passt (6 Spaces für `- name:`). Optional, falls verfügbar: `npx yaml-lint .github/workflows/deploy.yml`.

- [ ] **Step 6: Commit**

```bash
git add .github/workflows/deploy.yml .githooks/pre-push
git commit -m "ci(planer): vitest-Gate in deploy.yml + pre-push-Hook"
```

---

## Task 6: HANDOFF aktualisieren + Gesamt-Verifikation

**Files:**
- Modify: `Unterrichtsplaner/HANDOFF.md`

- [ ] **Step 1: Gesamt-Verifikation**

Run (aus `Unterrichtsplaner/`): `npx tsc -b && npm test && npm run build`
Expected: tsc Exit 0; `npm test` grün (alle drei Testdateien); Build erfolgreich.

- [ ] **Step 2: `HANDOFF.md` aktualisieren**

In `Unterrichtsplaner/HANDOFF.md`:
- In der Tabelle „Offene Punkte" die Zeile **UP-7** als erledigt markieren
  (Stil wie UP-5/UP-6: `~~UP-7~~` + `**… ✅ erledigt …**`).
- Unter „Letzte Sessions" einen Eintrag für UP-7 ergänzen: vitest-Setup
  (ExamLab-Parität), Unit-Tests für `generateColorVariants` / `gradeRequirements.ts`
  / `hkRotation.ts`, Clamp-Bugfix in `generateColorVariants`, CI-Gate in
  `deploy.yml` + `pre-push`.
- Header/Versionszeile konsistent nachziehen, falls die HANDOFF-Konvention das
  vorsieht.

- [ ] **Step 3: Commit**

```bash
git add Unterrichtsplaner/HANDOFF.md
git commit -m "docs(planer): HANDOFF — UP-7 erledigt (vitest-Setup + Tests)"
```

---

## Abschluss

Nach Task 6 ist der Branch `feature/up7-vitest-tests` fertig: vitest-Setup steht,
drei Testdateien grün, der `generateColorVariants`-Bug ist behoben, und `npm test`
ist CI-Gate. Merge nach `main` läuft über den regulären Projekt-Workflow
(`.claude/rules/deployment-workflow.md` + `regression-prevention.md`) — Browser-Test
ist hier nicht nötig (keine UI-Änderung ausser der Farb-Berechnung; diese ist durch
Tests abgedeckt), aber `npm run build` muss grün sein und die HANDOFF.md aktuell.
